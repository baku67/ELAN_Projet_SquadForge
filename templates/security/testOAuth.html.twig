{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}

    {# Erreur access_token lors Refresh #}
    <span>Test token session: {{ testToken }}</span>
    <span>Test $_GET code param: {{ testGetCodeParam }}</span>

    <p>Id de l'user from pseudo "nujabb"</p>
    {{ dump(getUser("nujabb")) }}
    {# {{ dump(getUserId("nujabb").data[0].id) }} #}


    {# Fonctionne ('mistermv') #}
    <p>Recherche des channels par mot</p>
    {{ dump(findChannel("mistermv")) }}

    <br><br>

    {# Fonctionne ('mario') mais pas tout les jeux #}
    <p>Recherche d'un jeu</p>
    {{ dump(findGamesByName("fortnite")) }}


    <br><br>

    <p>Recherche des channels par jeu_id</p>
    {{ dump(findGameChannels(33214)) }} 



    

{% endblock %}